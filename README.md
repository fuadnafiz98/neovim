# Neovim Config

> 💡 Make sure you have `xclip` installed for the integration of
> system clipboard with neovim.

```bash

$ mv ~/.config/nvim ~/.config/nvim-copy
$ git clone https://github.com/fuadnafiz98/neovim ~/.config/nvim

```
